Drop a short video here named `sample.mp4` to enable in-browser playback in
fixture/demo mode.

Without it, the waveform, transcript, and agent panel all still work — only the
<video> element shows a placeholder. Person A commits the real sample.mp4 for
the demo (see root Plan.md §8 / §14).
